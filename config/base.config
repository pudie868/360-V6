# ================== 目标平台 ==================
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq60xx=y
CONFIG_TARGET_qualcommax_ipq60xx_DEVICE_qihoo_360v6=y

# ================== 镜像设置 ==================
CONFIG_TARGET_ROOTFS_PARTSIZE=512
CONFIG_TARGET_ROOTFS_SQUASHFS=y

# ================== 核心冲突修复 ==================
# 显式禁用默认 dnsmasq，否则 dnsmasq-full 无法安装
CONFIG_PACKAGE_dnsmasq=n
CONFIG_PACKAGE_dnsmasq-full=y

# ================== NSS 硬件加速 ==================
# 360V6 (IPQ6018) 必须启用 NSS 才能跑满千兆
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
CONFIG_PACKAGE_kmod-qca-nss-drv-pppoe=y
CONFIG_PACKAGE_kmod-qca-nss-drv-bridge-mgr=y

# ================== 网络与代理 ==================
CONFIG_PACKAGE_sing-box=y
CONFIG_PACKAGE_luci-app-homeproxy=y
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
CONFIG_PACKAGE_luci-theme-argon=y

# passwall（需要 feeds.conf 中已添加 passwall 源）
CONFIG_PACKAGE_luci-app-passwall=y
CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y

# ================== 容器支持 ==================
# 【注意】dockerd 与 kmod-qca-nss-drv-bridge-mgr 在 IPQ60xx 上存在已知冲突：
#         NSS bridge manager 会劫持 veth 接口，导致容器网络不通。
#         如需使用 Docker，请禁用 NSS bridge manager，或仅在不依赖 NSS 桥接时使用。
CONFIG_PACKAGE_dockerd=y
CONFIG_PACKAGE_luci-app-dockerman=y
CONFIG_PACKAGE_kmod-veth=y
# Docker + nftables 防火墙所需模块（OpenWrt 23.05 默认 nftables）
CONFIG_PACKAGE_kmod-nft-bridge=y

# ================== 系统优化 ==================
# ZRAM
CONFIG_PACKAGE_zram-swap=y
# 【修复】显式启用 zram 内核模块，否则 zram-swap 可能在部分内核上找不到模块
CONFIG_PACKAGE_kmod-zram=y
CONFIG_PACKAGE_kmod-crypto-zstd=y

# BBR 拥塞控制
CONFIG_PACKAGE_kmod-tcp-bbr=y

# ================== 无线驱动 ==================
CONFIG_PACKAGE_kmod-ath11k-ahb=y
CONFIG_PACKAGE_ath11k-firmware-ipq6018=y
